// Placeholder smoke test. The merged app's main entrypoint requires
// ProviderScope + a live AuthService and is not exercised here.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
